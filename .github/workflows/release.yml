name: Release

on:
  push:
    tags:
      - 'v*'
      - '[0-9]+.[0-9]+.[0-9]+'

jobs:
  release:
    uses: osaurus-ai/osaurus-tools/.github/workflows/build-plugin.yml@master
    permissions:
      contents: write
    secrets: inherit
